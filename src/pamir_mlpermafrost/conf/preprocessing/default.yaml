train_test_split:
  _target_: ${train_test_split_function}
  _partial_: true
  test_size: 0.2
  random_state: ${seed}
  loc: ${test_corner}
  shuffle: true
  stratify: null

training:
  _target_: pamir_mlpermafrost.data.processors.process_Xy
  _partial_: true
  features: ${features}
  target: ${target}

inference:
  _target_: pamir_mlpermafrost.data.processors.process_X
  _partial_: true
  features: ${features}
